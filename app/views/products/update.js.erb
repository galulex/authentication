$('.product-rating').replaceWith("<%= j(render partial: 'rating') %>")
$('#product_review_' + "<%= @product.user_review_id(current_user) %>" + ' .user-rating').html("<%= j(user_product_rating(@product, current_user)) %>")
