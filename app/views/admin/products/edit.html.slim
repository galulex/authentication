= simple_nested_form_for @product, url: admin_product_path(@product), method: :patch, multipart: true do |f|
  = render partial: 'admin/products/form', locals: { f: f }
  =' f.button :submit, t('save_draft'), name: :save
  =' f.button :submit
