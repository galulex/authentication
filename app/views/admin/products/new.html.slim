= simple_form_for @product, as: :product, url: admin_products_path, multipart: true do |f|
  = render partial: 'form', locals: { f: f }
  =' f.button :submit, t('save_draft'), name: :save
  =' f.button :submit
